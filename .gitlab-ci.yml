include:
  - 'https://gitlab.esss.lu.se/ics-infrastructure/gitlab-ci-yml/raw/master/PreCommit.gitlab-ci.yml'
  - 'https://gitlab.esss.lu.se/ics-infrastructure/gitlab-ci-yml/raw/master/ArtifactoryPyPI.gitlab-ci.yml'

.runner_tags: &runner_tags
  tags:
    - docker

variables:
  PIP_INDEX_URL: "https://artifactory.esss.lu.se/artifactory/api/pypi/pypi-virtual/simple"

stages:
  - check
  - build
  - test
  - release

test-conda-python36:
  <<: *runner_tags
  stage: test
  image: registry.esss.lu.se/ics-docker/miniconda
  before_script:
    - source /etc/profile.d/conda.sh
    - conda create -n test -y -c conda-e3-virtual python=3.6 essioc
    - conda activate test
    - pip install -r requirements-dev.txt
    - pip install .
  script:
    - run-iocsh -h
    - pytest -v tests

test-conda-python37:
  <<: *runner_tags
  stage: test
  image: registry.esss.lu.se/ics-docker/miniconda
  before_script:
    - source /etc/profile.d/conda.sh
    - conda create -n test -y -c conda-e3-virtual python=3.7 essioc
    - conda activate test
    - pip install -r requirements-dev.txt
    - pip install .
  script:
    - run-iocsh -h
    - pytest -v --cov=run_iocsh tests
