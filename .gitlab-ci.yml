---
include:
  - project: "ics-infrastructure/gitlab-ci-yml"
    file: "PreCommit.gitlab-ci.yml"
  - project: "ics-infrastructure/gitlab-ci-yml"
    file: "ArtifactoryPyPI.gitlab-ci.yml"
  - project: "ics-infrastructure/gitlab-ci-yml"
    file: "SonarScanner.gitlab-ci.yml"

default:
  tags:
    - docker

variables:
  PIP_INDEX_URL: "https://artifactory.esss.lu.se/artifactory/api/pypi/pypi-virtual/simple"

stages:
  - check
  - build
  - test
  - analyse
  - release
  - deploy

build-pypi-package:
  image: python:3.12
  before_script:
    - pip install build
  script:
    - python -m build

.test-conda:
  stage: test
  image: registry.esss.lu.se/ics-docker/miniconda
  before_script:
    - source /etc/profile.d/conda.sh
    - conda create -n test -y -c conda-e3-virtual python=$PYTHON_VERSION require
    - conda activate test
    - pip install .[dev]
  script:
    - run-iocsh -h
    - pytest -v tests

test-conda-python312:
  extends: .test-conda
  script:
    - run-iocsh -h
    - pytest -v --cov=run_iocsh --cov-report=term-missing --cov-report=xml tests
  coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
  artifacts:
    paths:
      - coverage.xml
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
  variables:
    PYTHON_VERSION: "3.12"

run-sonar-scanner:
  needs:
    - job: test-python312
      artifacts: true

pages:
  stage: deploy
  image: registry.esss.lu.se/ics-docker/sphinx
  script:
    - sphinx-build -M html docs docs/build
    - mv docs/build/html public
  artifacts:
    paths:
      - public
  only:
    - master
